{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "prisma generate && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
